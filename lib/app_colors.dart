import 'package:flutter/material.dart';

class AppColors {
  /// 主色（亮蓝）
  static const Color primary = Color(0xFF1E6BFF);

  /// 全局背景
  static const Color background = Colors.white;

  /// Domain 九宫格卡片浅蓝底
  static const Color cardBlue = Color(0xFFDDEBFF);

  /// 副标题亮蓝（与主色一致，便于统一）
  static const Color subtitleBlue = primary;

  /// Check-in 提示条背景（更轻、更接近白）
  static const Color checkinBg = Color(0xFFF2F5FA);
}